apiVersion: v1
kind: Service

metadata:
  name: mlops-inference
  namespace: mlops

spec:
  type: LoadBalancer

  selector:
    app: mlops-inference

  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP